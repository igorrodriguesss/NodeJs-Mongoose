<h1>Editar Produto</h1>
<form method="post" action="/products/edit">
<Input type="hidden" name="id" value="{{product._id}}">
    <div class="form-control">
        <label for="name">Nome do produto:</label>
        <input type="text" name="name" placeholder="Digite o nome do produto" value="{{product.name}}">
    </div>
        <div class="form-control">
            <label for="image">Digite a URL da imagem</label>
            <input type="text" name="image" placeholder="Preço do produto:" value="{{product.image}}">
        </div>

        <div class="form-control">
            <label for="price">Digite o preço do produto:</label>
            <input type="text" name="price" placeholder="Preço do produto:" value="{{product.price}}">
        </div>

        <div class="form-control">
            <label for="description">Descrição do produto:</label>
            <textarea name="description" placeholder="digite a descrição do produto:">{{product.description}}</textarea> 
        </div>
    <input type="submit" value="Editar" class="btn">
</form>